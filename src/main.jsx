import React from "react";
import ReactDOM from "react-dom/client";
import QigongCalculator from "./QigongCalculator";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
    <React.StrictMode>
        <QigongCalculator />
    </React.StrictMode>
);
